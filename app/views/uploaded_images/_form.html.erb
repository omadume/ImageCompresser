<%= form_with(model: uploaded_image, local: true) do |form| %>
  <% if uploaded_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(uploaded_image.errors.count, "error") %> prohibited this uploaded_image from being saved:</h2>

      <ul>
        <% uploaded_image.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form_fields">
    <%= form.file_field :image %><br>
    <%= form.label :compression, "Compression level (percentage):" %>
    <%= form.number_field :compression, in: 0..100, required: true %>
  </div>

  <div class="form_actions mt-4">
    <%= form.submit "Upload image", :class =>"btn btn-primary" %>
  </div>
<% end %>
